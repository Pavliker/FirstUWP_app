<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AlbumApp1._0._1.Views.MailSendView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlbumApp1._0._1.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBox Text="{x:Bind ViewModel.Mail, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" PlaceholderText="Введите почту"/>
            <Button Command="{x:Bind ViewModel.SendMailCommand, Mode=OneWay}" Content="Отправить"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <TextBox IsEnabled="{x:Bind ViewModel.IsEnabled, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"  Text="{x:Bind ViewModel.Code,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" PlaceholderText="Введите проверочный код"/>
            <TextBox IsEnabled="{x:Bind ViewModel.IsEnabled, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Text="{x:Bind ViewModel.Pass,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" PlaceholderText="Введите новый пароль"/>
        </StackPanel>
    </Grid>
</UserControl>
