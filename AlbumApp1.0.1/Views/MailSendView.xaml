<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AlbumApp1._0._1.Views.MailSendView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlbumApp1._0._1.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

   

        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid>
                <StackPanel Grid.Row="0">
                    <TextBox Text="{x:Bind ViewModel.Mail, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" PlaceholderText="Введите почту"/>
                    <Button Margin="0 10 0 0" HorizontalAlignment="Center" Command="{x:Bind ViewModel.SendMailCommand, Mode=OneWay}" Content="Отправить"/>
                </StackPanel>
            </Grid>

            <Grid Margin="0 10 0 0" Grid.Row="1">
                <StackPanel  Grid.Row="1">
                    <TextBox Margin="0 0 0 10" IsEnabled="{x:Bind ViewModel.IsEnabled, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"  Text="{x:Bind ViewModel.Code,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" PlaceholderText="Введите проверочный код"/>
                    <TextBox IsEnabled="{x:Bind ViewModel.IsEnabled, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Text="{x:Bind ViewModel.Pass,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" PlaceholderText="Введите новый пароль"/>
                </StackPanel>
            </Grid>
        </Grid>
       
       
    
</UserControl>
