<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AlbumApp1._0._1.Views.Basic.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlbumApp1._0._1.Views.Basic"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid   >
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
        </Grid>
        <Grid Background="DarkGreen" Margin="5"  >
            <Grid  Grid.Row="0"  VerticalAlignment="Center">
                <Line Stroke="Aquamarine" StrokeThickness="2" Height="10" Width="10"/>
            </Grid>
            <Grid  Grid.Row="1"  VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Margin="10" Grid.Column="0" Width="Auto" Height="50" CornerRadius="100">
                    <Image  HorizontalAlignment="Center" Height="Auto" Width="Auto" Stretch ="UniformToFill"  Source="/Assets/Profile.png"/>
                </Button>
                <TextBlock  Grid.Column="1" Margin="10" VerticalAlignment="Center" Text="{x:Bind ViewModel.Username}" />

            </Grid>

            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2">
                <StackPanel  Margin="10"  Grid.Row="1">
                    <Button Command="{x:Bind ViewModel.NavigateToAllCommand}" BorderBrush="Black" BorderThickness="2" Margin="0 0 0 17" CornerRadius="19" Width="149" Height="37" Content="Все"/>
                    <Button Command="{x:Bind ViewModel.NavigateToAlbumsCommand}" BorderBrush="Black" BorderThickness="2" Margin="0 0 0 17" CornerRadius="19" Width="149" Height="37"  Content="Альбомы"/>
                    <Button Command="{x:Bind ViewModel.NavigateToFavouritesCommand}" BorderBrush="Black" BorderThickness="2" Margin="0 0 0 17"  CornerRadius="19" Width="149" Height="37" Content="Избранные"/>
                    <Button Command="{x:Bind ViewModel.RandomPhotoCommand}" BorderBrush="Black" BorderThickness="2" Margin="0 0 0 17"  CornerRadius="19" Width="149" Height="37" Content="Случайное фото"/>
                    <Button Command="{x:Bind ViewModel.NavigateToFeedbackCommand}" BorderBrush="Black" BorderThickness="2" Margin="0 0 0 17" CornerRadius="19" Width="149" Height="37" Content="Обратная связь"/>
                    <Button Command="{x:Bind ViewModel.NavigateToArchiveCommand}" BorderBrush="Black" BorderThickness="2" Margin="0 0 0 17"  CornerRadius="19" Width="149" Height="37" Content="Архив"/>
                    <Button Command="{x:Bind ViewModel.NavigateToQuestionsCommand}" BorderBrush="Black" BorderThickness="2" Margin="0 0 0 17" CornerRadius="19"   Width="149" Height="37" Content="Вопросы"/>
                    <Button Command="{x:Bind ViewModel.NavigateToAboutProjectCommand}" BorderBrush="Black" BorderThickness="2" Margin="0 0 0 17" CornerRadius="19" Width="149" Height="37" Content="О проекте"/>
                    <Button Command="{x:Bind ViewModel.ExitCommand}" BorderBrush="Black" BorderThickness="2" Margin="0 0 0 17"  CornerRadius="19" Width="149" Height="37"  Content="Выход"/>
                </StackPanel>
            </Grid>


            <Grid Grid.Row="3">
                <Grid.RowDefinitions>
                    <RowDefinition  Height="*" />
                    <RowDefinition Height="*"  />
                    <RowDefinition Height="0.5*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="3" VerticalAlignment="Center" Margin="10" Orientation="Horizontal">
                    <ComboBox PlaceholderText="Язык" Margin="0 0 10 0 ">

                    </ComboBox>
                    <ComboBox PlaceholderText="Тема">

                    </ComboBox>
                </StackPanel>
            </Grid>
       
        </Grid>

    </Grid>
</Page>
