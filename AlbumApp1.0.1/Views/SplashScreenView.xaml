<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AlbumApp1._0._1.Views.SplashScreenView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlbumApp1._0._1.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
   

    >
 

        <Grid  >
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

        <TextBlock x:Name="text" FontSize="24" Text="{x:Bind _splashScreenViewModel.Text, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Row="1"/>
        <ProgressBar   x:Name="progress1" Maximum="100"   Height="100" Value="{x:Bind _splashScreenViewModel.Value,  Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Grid.Row="2"/>
        </Grid>
</Page>
