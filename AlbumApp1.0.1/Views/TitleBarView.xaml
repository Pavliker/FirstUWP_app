<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AlbumApp1._0._1.Views.TitleBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlbumApp1._0._1.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>

            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
         

            </Grid.ColumnDefinitions>
            <StackPanel  Grid.Column="0" x:Name="AppTitleBar" 
              Orientation="Horizontal"
                         
              Margin="10"
              Grid.Row="0" 
              >
                <TextBlock x:Name="AppTitleTextBlock" Text="Album"
                  Grid.Column="0"
                  Grid.Row="0"
                  TextWrapping="NoWrap"
                 
                  VerticalAlignment="Top"
                  Margin="8,8,0,0"/>

            </StackPanel>

            <StackPanel Margin="10" HorizontalAlignment="Center"  Grid.Column="1"   Grid.Row="0" Orientation="Horizontal">
                <Button Margin="0 0 50 0 " Command="{x:Bind TitleBarViewModel._NavigateToAuthViewCommand ,Mode=TwoWay}" Width="150"  HorizontalContentAlignment="Center"   Content="Вход">

                </Button>
                <Button Command="{x:Bind TitleBarViewModel._NavigateToRegViewCommand, Mode=TwoWay}" Width="150"  Content="Регистрация"/>
            </StackPanel>


        </Grid>
        <!--<Grid VerticalAlignment="Center" Grid.Row="2" Grid.Column="1">
            <Frame VerticalAlignment="Center" Grid.Row="1" Content="{x:Bind ViewModel.navigationService.Frame, Mode=OneWay}"  />
        </Grid>-->
    </StackPanel>

</Page>
