<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AlbumApp1._0._1.Views.SplashScreenView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlbumApp1._0._1.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
   

    >
 

        <Grid>
            <Grid.RowDefinitions>
             <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
            
        <ContentControl Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" Grid.ColumnSpan="3" Template="{StaticResource SplashScreenTemplate}" />
        
        <StackPanel Grid.Row="1" Grid.Column="1" >
            <ProgressRing HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Green"  Background="LightGreen"    Maximum="100" Width="Auto"   Height="Auto" MaxWidth="210" MaxHeight="210" Value="{x:Bind _splashScreenViewModel.Value,  Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  Grid.Row="1" Grid.Column="1"  IsIndeterminate="False"/>
            <TextBlock Foreground="Gray" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="1" FontSize="24" Text="{x:Bind _splashScreenViewModel.Text, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Column="1"/>
        </StackPanel>




    </Grid>
</Page>
