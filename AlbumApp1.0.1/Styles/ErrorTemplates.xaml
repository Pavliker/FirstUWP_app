<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:x1="using:System"
    xmlns:VM="using:AlbumApp1._0._1.ViewModels"
        xmlns:tm="using:AlbumApp1._0._1.Styles.Templates"
xmlns:local="using:AlbumApp1._0._1.Models"
    >
    <!--<Style TargetType="ContentControl">
        <Style.Setters></Style.Setters>
            <.Value>
                <Grid></Grid>
                -->
    <!--<Border Background="Red"   Visibility="{Binding Пользователи.HasErrors, Mode=OneWay}">

                </Border>
                <ToolTip>
                    -->
    <!--<TextBlock Text="{Binding Пользователи.Errors[0].ErrorContent, Mode=OneWay}" Foreground="Red">

                    </TextBlock>-->
    <!--
                </ToolTip>-->
    <!--
            </.Value>
            
        </>
    </Style>-->

    

   


    <!--<Style TargetType="TextBox">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid>
                            <Border Background="Red"   Visibility="{Binding Пользователи.HasErrors, Mode=OneWay}">

                            </Border>
                            <ToolTip>

                                <TextBlock Text="{Binding Validation.Error[0].ErrorContent, Mode=OneWay}" Foreground="Red">

                                </TextBlock>
                            </ToolTip>
                        </Grid>


                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>-->
</ResourceDictionary>