<Page
    x:Class="AlbumApp1._0._1.Views.MainPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:tm="using:AlbumApp1._0._1.Styles.Templates" 
    >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="2*"/>

        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Grid.ColumnSpan="2">
            <ContentPresenter     ContentTemplate="{StaticResource _TitleBarViewModel}" ContentTemplateSelector="{StaticResource CustomTemplateSelector }"  />

        </Grid>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Grid.RowSpan="2" Grid.Column="1" Grid.Row="1" >
            <Frame  Content="{x:Bind ViewModel.NavigationService.Frame, Mode=OneWay}"/>
        </Grid>
    </Grid>



    <!--ContentTemplateSelector="{StaticResource CustomTemplateSelector}"-->

    

</Page>
<!--xmlns:viewmodels="clr-namespace:AlbumApp1._0._1.ViewModels"
d:Data="{d:DesignInstance Type=viewmodels:MainViewModel}"-->