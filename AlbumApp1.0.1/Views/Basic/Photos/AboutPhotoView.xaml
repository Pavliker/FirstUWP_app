<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AlbumApp1._0._1.Views.Basic.Photos.AboutPhotoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlbumApp1._0._1.Views.Basic.Photos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!--<ControlTemplate x:Key="btnTemplate" TargetType="Button">
            <Grid>
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                

                        <VisualState x:Name="PointerOver">
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).Color"  Duration="Forever" From="AliceBlue"  To="Red"/>

                             
                            </Storyboard>
                        </VisualState>

                    </VisualStateGroup>

                </VisualStateManager.VisualStateGroups>
                <Border Background="White" Width="100" Name="border" >
                    <Grid>
                        <TextBlock Foreground="Black" Text="sdff"/>
                    </Grid>
                </Border>
            </Grid>
       
        </ControlTemplate>-->
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid HorizontalAlignment="Center" Margin="10" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Width="Auto">
                <Image Source="{x:Bind ViewModel.Photos.Image}"/>
            </Grid>
            <Grid Padding="10" Background="Gray" HorizontalAlignment="Center"  Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>

                </Grid.RowDefinitions>
                  
                   
                <Button  HorizontalAlignment="Center" Grid.Row="0" Width="110" Height="35">

               
                  
                </Button>
                <Button Grid.Row="1" Content="Печать"  Width="110" Height="35"/>
                <Button Grid.Row="2" Content="Поделиться" Width="110" Height="35"/>

            </Grid>

        </Grid>
        <Grid Grid.Column="1">
                

                <Grid Background="LightGray">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock/>

                <TextBlock Grid.Row="1"/>
                </Grid>
            
        </Grid>
        <Grid Grid.Row="1">
            <StackPanel>
                <TextBlock/>
                <TextBlock/>
                <TextBlock/>
                <TextBlock/>
                <TextBlock/>
                <TextBlock/>
                <TextBlock/>

            </StackPanel>
        </Grid>
     
    </Grid>
</Page>
